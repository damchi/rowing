import { EntrainementsPlanning } from '../entrainements-planning';

describe('EntrainementsPlanning', () => {
  it('should create an instance', () => {
    expect(new EntrainementsPlanning()).toBeTruthy();
  });
});
